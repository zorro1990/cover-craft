# Cover Craft 部署指南

## 目录
1. [环境准备](#环境准备)
2. [本地开发](#本地开发)
3. [生产构建](#生产构建)
4. [Vercel 部署](#vercel-部署)
5. [其他平台部署](#其他平台部署)
6. [环境变量](#环境变量)
7. [性能优化](#性能优化)
8. [监控与日志](#监控与日志)
9. [故障排除](#故障排除)

---

## 环境准备

### 系统要求
- **Node.js**: v18.0.0 或更高版本
- **npm**: v9.0.0 或更高版本
- **操作系统**: Windows、macOS、Linux

### 检查环境
```bash
node --version
npm --version
```

### 安装依赖
```bash
# 在项目根目录
npm install

# 或在 apps/web 目录
cd apps/web
npm install
```

---

## 本地开发

### 启动开发服务器
```bash
# 从项目根目录启动
npm run dev

# 或指定端口
npm run dev -- --port 3000
```

### 访问应用
开发服务器启动后，打开浏览器访问：
```
http://localhost:3000
```

### 开发命令
```bash
# 启动开发服务器
npm run dev

# 运行测试
npm run test

# 代码检查
npm run lint

# 类型检查
npm run type-check
```

---

## 生产构建

### 构建应用
```bash
# 在 apps/web 目录
cd apps/web
npm run build
```

### 预览构建结果
```bash
npm run start
```

访问 `http://localhost:3000` 查看构建后的应用。

### 构建输出
构建完成后，会在 `apps/web/.next/` 目录生成生产文件。

---

## Vercel 部署 (推荐)

Vercel 是 Next.js 的创建者推荐的部署平台。

### 步骤 1: 安装 Vercel CLI
```bash
npm install -g vercel
```

### 步骤 2: 登录 Vercel
```bash
vercel login
```

### 步骤 3: 部署项目
```bash
# 在项目根目录
vercel

# 或部署到生产环境
vercel --prod
```

### 步骤 4: 配置环境变量
在 Vercel Dashboard 中设置环境变量：
1. 进入项目设置
2. 点击 "Environment Variables"
3. 添加必要的环境变量

### 自动部署
连接到 GitHub 后，每次推送代码会自动触发部署。

---

## 其他平台部署

### Netlify

1. 构建命令: `npm run build`
2. 发布目录: `apps/web/.next`
3. 环境变量: 在 Netlify Dashboard 中设置

### AWS Amplify

1. 连接到 Git 仓库
2. 构建命令: `npm run build`
3. 输出目录: `.next`

### Docker 部署

创建 `Dockerfile`:
```dockerfile
FROM node:18-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci --only=production

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "start"]
```

构建镜像:
```bash
docker build -t cover-craft .
```

运行容器:
```bash
docker run -p 3000:3000 cover-craft
```

---

## 环境变量

### 必需变量
```bash
NODE_ENV=production
```

### 可选变量
```bash
# 应用信息
NEXT_PUBLIC_APP_NAME=Cover Craft
NEXT_PUBLIC_APP_VERSION=1.0.0

# API 配置
NEXT_PUBLIC_API_URL=https://api.example.com

# 错误监控
SENTRY_DSN=your_sentry_dsn

# 分析
NEXT_PUBLIC_GA_ID=your_google_analytics_id
```

### 设置环境变量

**本地开发**:
创建 `.env.local` 文件:
```bash
cp .env.example .env.local
```

**Vercel**:
在 Dashboard 中设置

**其他平台**:
参考平台文档

---

## 性能优化

### 构建优化

检查构建输出:
```bash
npm run build -- --analyze
```

### 部署优化

1. **启用压缩**: Vercel 自动启用
2. **缓存策略**: 配置合适的缓存头
3. **CDN**: 使用 CDN 加速静态资源
4. **图片优化**: Next.js 自动优化

### 监控指标

关键性能指标 (KPI):
- **首次加载**: < 2 秒
- **交互延迟**: < 100ms
- **CLS 分数**: < 0.1
- **LCP 分数**: < 2.5s

---

## 监控与日志

### 错误监控

集成 Sentry:
```bash
npm install @sentry/nextjs
```

配置 `sentry.client.config.js`:
```javascript
import * as Sentry from '@sentry/nextjs'

Sentry.init({
  dsn: process.env.SENTRY_DSN,
})
```

### 性能监控

Vercel Analytics:
```bash
npm install @vercel/analytics
```

Google Analytics:
```bash
npm install @next/third-parties
```

### 日志记录

使用结构化日志:
```javascript
console.log(JSON.stringify({
  level: 'info',
  message: 'User action',
  timestamp: new Date().toISOString(),
}))
```

---

## 故障排除

### 构建失败

**问题**: TypeScript 错误
```bash
# 解决方案
npm run type-check
```

**问题**: 依赖缺失
```bash
# 解决方案
npm install
npm run build
```

### 运行时错误

**问题**: 页面空白
- 检查控制台错误
- 验证环境变量
- 查看 Vercel 函数日志

**问题**: 加载缓慢
- 检查网络连接
- 验证 CDN 配置
- 优化图片大小

### 常见错误码

| 错误码 | 含义 | 解决方案 |
|--------|------|----------|
| 500 | 服务器错误 | 查看函数日志 |
| 404 | 页面不存在 | 检查路由配置 |
| 403 | 权限不足 | 验证访问权限 |

---

## 最佳实践

### 部署检查清单

- [ ] 代码通过所有测试
- [ ] 无 TypeScript 错误
- [ ] ESLint 检查通过
- [ ] 环境变量已配置
- [ ] 错误监控已启用
- [ ] 性能监控已配置
- [ ] 文档已更新

### 安全检查

- [ ] 移除敏感信息
- [ ] 使用环境变量
- [ ] 启用 HTTPS
- [ ] 配置 CSP 头
- [ ] 定期更新依赖

### 监控检查

- [ ] 错误率 < 1%
- [ ] 响应时间 < 500ms
- [ ] 可用性 > 99.9%
- [ ] 崩溃率 < 0.1%

---

## 团队协作

### Git 工作流
1. 创建功能分支
2. 提交代码
3. 创建 Pull Request
4. 代码审查
5. 合并到主分支
6. 自动部署

### 分支策略
- `main`: 生产分支
- `develop`: 开发分支
- `feature/*`: 功能分支
- `hotfix/*`: 修复分支

### 发布流程
1. 合并到 `main` 分支
2. 创建 Release Tag
3. 自动部署到生产
4. 验证部署结果
5. 更新文档

---

## 联系支持

- **文档**: 查看项目 README
- **问题**: 提交 GitHub Issue
- **讨论**: 参与 GitHub Discussions
- **紧急问题**: 联系维护者

---

## 更新日志

### v1.0.0 (2025-11-02)
- 初始部署配置
- Vercel 集成
- 环境变量管理
- 监控配置

### 计划更新
- 自动化测试部署
- 蓝绿部署
- 多环境支持
- 回滚机制

---

感谢阅读！🚀
